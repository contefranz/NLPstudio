% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stem_tokens.R
\name{stem_tokens}
\alias{stem_tokens}
\title{Stem Tokens}
\usage{
stem_tokens(
  x,
  language = "english",
  ncores = 1,
  nchunks = ncores,
  socket = c("PSOCK", "FORK")
)
}
\arguments{
\item{x}{A \link[quanteda:tokens]{quanteda::tokens} object.}

\item{language}{Character. Snowball stemmer language. Defaults to \code{"english"}.
See \code{\link[SnowballC:getStemLanguages]{SnowballC::getStemLanguages()}} for the full list.}

\item{ncores}{Integer. Number of CPU cores to use for parallel
processing. Defaults to 1 (sequential).}

\item{nchunks}{Integer. Number of chunks to split the corpus into.
Defaults to \code{ncores}. Setting \code{nchunks > ncores} can improve load
balancing when documents vary in size. See \emph{Details}.}

\item{socket}{Character. Parallel backend to use. One of \code{"PSOCK"}
(default, recommended) or \code{"FORK"}. On Windows, \code{"FORK"} is not
supported and will trigger an error.}
}
\value{
A \link[quanteda:tokens]{quanteda::tokens} object with stemmed tokens.
}
\description{
Reduce tokens to their stems using the Snowball stemmer, in parallel. This is a
parallel-aware wrapper around \code{\link[quanteda:char_wordstem]{quanteda::char_wordstem()}} applied to the token
vocabulary, so it scales to large corpora. Unlike \code{\link[=singularize_tokens]{singularize_tokens()}}
(English plurals only), stemming supports many languages via Snowball.
}
\details{
Stemming is applied once per unique token type and then mapped back onto the
tokens with \code{\link[quanteda:tokens_replace]{quanteda::tokens_replace()}}, mirroring the strategy used in
\code{\link[=singularize_tokens]{singularize_tokens()}}. See \code{\link[=tokenize_corpus]{tokenize_corpus()}} for the parallel backend.
}
\examples{
corp <- quanteda::corpus(c(
  doc1 = "running runners ran easily",
  doc2 = "computational computers compute"
))
toks <- tokenize_corpus(corp)
stem_tokens(toks)

}
\seealso{
\code{\link[=singularize_tokens]{singularize_tokens()}}, \code{\link[=lemmatize_tokens]{lemmatize_tokens()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm_interpretation.R
\name{estimate_stm_topic_effects}
\alias{estimate_stm_topic_effects}
\title{Estimate STM Topic Effects}
\usage{
estimate_stm_topic_effects(
  fit,
  formula = NULL,
  metadata = NULL,
  topics = NULL,
  uncertainty = c("Global", "Local", "None"),
  nsims = 25L,
  conf_level = 0.95
)
}
\arguments{
\item{fit}{An STM \code{nlp_topic_fit} returned by \code{\link[=fit_topic_model]{fit_topic_model()}} or a raw
\strong{stm} \code{STM} object without content covariates.}

\item{formula}{Optional prevalence formula. If \code{NULL}, NLPstudio uses the
prevalence formula stored in the STM fit. A right-hand-side-only formula
such as \code{~ group} is combined with the selected topics. A full formula is
forwarded as-is.}

\item{metadata}{Optional metadata for \code{\link[stm:estimateEffect]{stm::estimateEffect()}}. If omitted,
stored \code{fit$docvars} are used when available.}

\item{topics}{Optional topic filter supplied as numeric topic indices or
\code{Topic###} identifiers. Ignored when \code{formula} is a full formula with its
own left-hand side.}

\item{uncertainty}{Uncertainty mode forwarded to \code{\link[stm:estimateEffect]{stm::estimateEffect()}}.
One of \code{"Global"}, \code{"Local"}, or \code{"None"}.}

\item{nsims}{Integer number of simulations forwarded to
\code{\link[stm:estimateEffect]{stm::estimateEffect()}}. Defaults to \code{25L}.}

\item{conf_level}{Confidence level used for Wald intervals in the returned
table. Defaults to \code{0.95}.}
}
\value{
An object of class
\code{c("nlp_stm_topic_effects", "data.table", "data.frame")} with tidy
coefficient rows. The raw \code{estimateEffect} object is attached as
\code{attr(result, "estimate_effect")}.
}
\description{
Estimate and tidy STM prevalence effects for fitted topics.
}
\details{
This helper reports prevalence effects for STM fits. It does not add new STM
prediction behavior, and it does not support content-covariate STM models.
}
\examples{
\dontshow{if (requireNamespace("stm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
texts <- c(
  doc1 = "profit revenue growth",
  doc2 = "profit margin growth",
  doc3 = "risk litigation loss",
  doc4 = "debt risk loss",
  doc5 = "revenue market profit",
  doc6 = "litigation cost risk"
)
corp <- quanteda::corpus(texts)
quanteda::docvars(corp, "group") <- rep(c("a", "b"), 3)
dfm <- quanteda::dfm(quanteda::tokens(corp))
fit <- fit_topic_model(
  dfm,
  engine = "stm",
  model = "stm",
  k = 2,
  docvars = TRUE,
  control = list(
    fit = list(
      prevalence = ~ group,
      seed = 1,
      max.em.its = 5,
      verbose = FALSE
    )
  )
)
estimate_stm_topic_effects(fit, nsims = 5)
\dontshow{\}) # examplesIf}
}

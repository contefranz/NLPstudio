% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compound_tokens.R
\name{compound_tokens}
\alias{compound_tokens}
\title{Compound Multiword Expressions into Single Tokens}
\usage{
compound_tokens(
  x,
  pattern,
  concatenator = "_",
  ncores = 1,
  nchunks = ncores,
  socket = c("PSOCK", "FORK"),
  ...
)
}
\arguments{
\item{x}{A \link[quanteda:tokens]{quanteda::tokens} object.}

\item{pattern}{Phrases to compound. One of: a character vector of phrases
(e.g. \code{c("annual report", "cash flow")}); a \code{data.frame}/\code{data.table} with a
\code{collocation} column, such as the output of \code{\link[=detect_collocations]{detect_collocations()}}; or a
\strong{quanteda} collocations object.}

\item{concatenator}{Character used to join compounded tokens. Defaults to
\code{"_"}.}

\item{ncores}{Integer. Number of CPU cores to use for parallel
processing. Defaults to 1 (sequential).}

\item{nchunks}{Integer. Number of chunks to split the corpus into.
Defaults to \code{ncores}. Setting \code{nchunks > ncores} can improve load
balancing when documents vary in size. See \emph{Details}.}

\item{socket}{Character. Parallel backend to use. One of \code{"PSOCK"}
(default, recommended) or \code{"FORK"}. On Windows, \code{"FORK"} is not
supported and will trigger an error.}

\item{...}{Additional arguments passed to \code{\link[quanteda:tokens_compound]{quanteda::tokens_compound()}}.}
}
\value{
A \link[quanteda:tokens]{quanteda::tokens} object with matched phrases compounded, in the
same document order as the input.
}
\description{
Join sequences of tokens (phrases or detected collocations) into single
compound tokens, in parallel. Wraps \code{\link[quanteda:tokens_compound]{quanteda::tokens_compound()}} and composes
directly with \code{\link[=detect_collocations]{detect_collocations()}}.
}
\details{
The tokens object is split into balanced chunks by document and processed with
the package's shared parallel backend. See \code{\link[=tokenize_corpus]{tokenize_corpus()}} for details.
}
\examples{
corp <- quanteda::corpus(c(
  doc1 = "the annual report described cash flow risk",
  doc2 = "annual report disclosures mention cash flow"
))
toks <- tokenize_corpus(corp)
compound_tokens(toks, pattern = c("annual report", "cash flow"))

}
\seealso{
\code{\link[=detect_collocations]{detect_collocations()}}, \code{\link[=ngram_tokens]{ngram_tokens()}}
}

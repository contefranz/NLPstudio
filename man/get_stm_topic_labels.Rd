% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm_interpretation.R
\name{get_stm_topic_labels}
\alias{get_stm_topic_labels}
\title{Extract STM Topic Labels}
\usage{
get_stm_topic_labels(
  x,
  n = 7L,
  topics = NULL,
  label_types = c("prob", "frex", "lift", "score"),
  frexweight = 0.5,
  include_sage = FALSE
)
}
\arguments{
\item{x}{An STM \code{nlp_topic_fit} returned by \code{\link[=fit_topic_model]{fit_topic_model()}} or a raw
\strong{stm} \code{STM} object without content covariates.}

\item{n}{Integer. Number of terms per label type. Defaults to \code{7L}.}

\item{topics}{Optional topic filter supplied as numeric topic indices or
\code{Topic###} identifiers.}

\item{label_types}{Character vector of STM label families to return. Valid
values are \code{"prob"}, \code{"frex"}, \code{"lift"}, and \code{"score"}.}

\item{frexweight}{Numeric value in \verb{[0, 1]} forwarded to
\code{\link[stm:labelTopics]{stm::labelTopics()}} for FREX labels. Defaults to \code{0.5}.}

\item{include_sage}{Logical. Should \code{\link[stm:sageLabels]{stm::sageLabels()}} marginal labels also
be included? Defaults to \code{FALSE}.}
}
\value{
A \link[data.table:data.table]{data.table} with columns \code{topic_id},
\code{topic_int}, \code{source}, \code{label_type}, \code{rank}, and \code{term}.
}
\description{
Return STM-native topic labels as a standardized long table. The helper wraps
\code{\link[stm:labelTopics]{stm::labelTopics()}} and, optionally, \code{\link[stm:sageLabels]{stm::sageLabels()}} while keeping
NLPstudio's canonical \code{Topic###} identifiers.
}
\details{
This function is STM-specific. It is meant to complement the engine-agnostic
\code{\link[=get_top_terms]{get_top_terms()}} accessor when users want labels based on STM's own
probability, FREX, lift, score, and optional SAGE calculations.

STM content-covariate models are not supported because they imply
covariate-specific topic-word distributions, while NLPstudio currently
standardizes one TWW matrix per fit.
}
\examples{
\dontshow{if (requireNamespace("stm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 2, 0, 0,  0, 0, 2, 1,
             0, 0, 1, 2,  2, 1, 0, 0,  0, 0, 1, 2),
           nrow = 6, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:6)
colnames(dtm) <- c("growth", "profit", "risk", "loss")
fit <- fit_topic_model(
  dtm,
  engine = "stm",
  model = "stm",
  k = 2,
  control = list(fit = list(seed = 1, max.em.its = 5, verbose = FALSE))
)
get_stm_topic_labels(fit, n = 3)
\dontshow{\}) # examplesIf}
}

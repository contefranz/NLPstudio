% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{get_topic_embeddings}
\alias{get_topic_embeddings}
\title{Extract ETM Topic Embeddings}
\usage{
get_topic_embeddings(x)
}
\arguments{
\item{x}{Either an \code{nlp_topic_fit} with \code{engine = "topicmodels.etm"} or a raw
ETM object.}
}
\value{
A \link[data.table:data.table]{data.table} with one row per topic,
a standardized \code{topic_id} column using the \code{Topic###} convention, and one
column per embedding dimension named \code{dim_001}, \code{dim_002}, and so on.
}
\description{
Extract topic-center embeddings from an embedded topic model (ETM).
}
\examples{
\dontshow{if (requireNamespace("topicmodels.etm", quietly = TRUE) && requireNamespace("torch", quietly = TRUE) && torch::torch_is_installed()) withAutoprint(\{ # examplesIf}
path <- system.file(package = "topicmodels.etm", "example", "example_etm.ckpt")
model <- torch::torch_load(path)
get_topic_embeddings(model)
\dontshow{\}) # examplesIf}
}

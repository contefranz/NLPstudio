% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{get_dtw}
\alias{get_dtw}
\title{Extract Standardized Document Topic Weights}
\usage{
get_dtw(
  x,
  doc_data = NULL,
  include_text = FALSE,
  doc_id_col = "doc_id",
  text_col = "text"
)
}
\arguments{
\item{x}{A supported topic-model object. This includes \code{nlp_topic_fit},
\code{warp_lda()} output, raw \code{topicmodels} fits, raw \code{seededlda} fits, and
already standardized DTW tables.}

\item{doc_data}{Optional document-data override. When supplied, this is used
instead of any \code{doc_data} stored in \code{x}. Accepted inputs are a corpus,
data.frame, or data.table keyed by \code{doc_id}.}

\item{include_text}{Should a \code{text} column be attached when a text-bearing
\code{doc_data} source is available? Defaults to \code{FALSE}. When \code{TRUE} but no
text-bearing \code{doc_data} is available, the function emits a warning.}

\item{doc_id_col}{Document-ID column name when \code{doc_data} is a data.frame or
data.table. Defaults to \code{"doc_id"}.}

\item{text_col}{Text column name when \code{doc_data} is a data.frame or
data.table. Defaults to \code{"text"}.}
}
\value{
A \code{data.table} with:
\itemize{
\item \code{doc_id}
\item topic columns named \code{Topic001}, \code{Topic002}, ...
\item \code{topic_max_id}
\item \code{topic_max_value}
\item metadata columns when available
\item \code{text} when \code{include_text = TRUE} and text is available
}
}
\description{
Extract DTW (document-topic weights) from a supported topic-model object and
return a standardized \link[data.table:data.table]{data.table}.
}
\examples{
dtm <- methods::as(
  Matrix::Matrix(
    matrix(
      c(1, 0, 1,
        1, 1, 0,
        0, 1, 1,
        1, 1, 1),
      nrow = 4,
      byrow = TRUE
    ),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:4)
colnames(dtm) <- paste0("term", 1:3)

fit <- fit_topic_model(
  dtm,
  engine = "text2vec",
  model = "lda",
  k = 2,
  control = list(fit = list(n_iter = 25, progressbar = FALSE))
)

get_dtw(fit)

}

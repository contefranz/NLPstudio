% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lemmatize_tokens.R
\name{lemmatize_tokens}
\alias{lemmatize_tokens}
\title{Lemmatize Tokens}
\usage{
lemmatize_tokens(
  x,
  engine = c("lookup", "spacy"),
  lemma = NULL,
  ncores = 1,
  nchunks = ncores,
  socket = c("PSOCK", "FORK"),
  ...
)
}
\arguments{
\item{x}{A \link[quanteda:tokens]{quanteda::tokens} object.}

\item{engine}{Character. \code{"lookup"} (default) applies the \code{lemma} map;
\code{"spacy"} derives lemmas using \strong{spacyr} (requires a working spaCy
installation and an initialized model; see \code{\link[=parse_corpus]{parse_corpus()}}).}

\item{lemma}{For \code{engine = "lookup"}, the lemma map: either a named character
vector (\code{c(token = "lemma")}) or a \code{data.frame}/\code{data.table} with columns
\code{token} and \code{lemma}. Ignored for \code{engine = "spacy"}.}

\item{ncores}{Integer. Number of CPU cores to use for parallel
processing. Defaults to 1 (sequential).}

\item{nchunks}{Integer. Number of chunks to split the corpus into.
Defaults to \code{ncores}. Setting \code{nchunks > ncores} can improve load
balancing when documents vary in size. See \emph{Details}.}

\item{socket}{Character. Parallel backend to use. One of \code{"PSOCK"}
(default, recommended) or \code{"FORK"}. On Windows, \code{"FORK"} is not
supported and will trigger an error.}

\item{...}{For \code{engine = "spacy"}, additional arguments passed to
\code{\link[=parse_corpus]{parse_corpus()}} / \code{\link[spacyr:spacy_parse]{spacyr::spacy_parse()}}.}
}
\value{
A \link[quanteda:tokens]{quanteda::tokens} object with lemmatized tokens.
}
\description{
Replace tokens with their lemmas. Two engines are available: a dependency-free
\code{"lookup"} engine that applies a user-supplied lemma map, and a \code{"spacy"}
engine that derives lemmas with the optional \strong{spacyr} backend. In both cases
lemmas are applied at the token-type level via \code{\link[quanteda:tokens_replace]{quanteda::tokens_replace()}}.
}
\details{
Lemmatization here is type-level: each distinct token form is mapped to a
single lemma and substituted with \code{\link[quanteda:tokens_replace]{quanteda::tokens_replace()}}. The \code{"spacy"}
engine builds that map from a spaCy parse, taking the most frequent lemma per
token form, so it approximates fully context-sensitive lemmatization. For
richly context-dependent analyses, parse the corpus directly with
\code{\link[=parse_corpus]{parse_corpus()}} and work from the returned annotations.

The parallel arguments (\code{ncores}, \code{nchunks}, \code{socket}) apply only to the
\code{"spacy"} engine's parsing step.
}
\examples{
corp <- quanteda::corpus(c(
  doc1 = "mice were running",
  doc2 = "the geese are flying"
))
toks <- tokenize_corpus(corp)

# Dependency-free lookup engine with a small custom map
lemma_map <- data.frame(
  token = c("mice", "were", "running", "geese", "are", "flying"),
  lemma = c("mouse", "be", "run", "goose", "be", "fly")
)
lemmatize_tokens(toks, engine = "lookup", lemma = lemma_map)

}
\seealso{
\code{\link[=stem_tokens]{stem_tokens()}}, \code{\link[=singularize_tokens]{singularize_tokens()}}, \code{\link[=parse_corpus]{parse_corpus()}}
}

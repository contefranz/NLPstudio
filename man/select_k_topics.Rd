% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_k_topics.R
\name{select_k_topics}
\alias{select_k_topics}
\title{Select the Number of Topics by Grid Search}
\usage{
select_k_topics(
  x,
  engine,
  model,
  k_grid = 5:15,
  metrics = c("coherence_npmi", "coherence_umass", "diversity", "exclusivity",
    "held_out_nll", "held_out_perplexity", "train_nll", "train_perplexity"),
  level = c("aggregate", "topic", "all"),
  control = list(),
  holdout = 0.2,
  ncores = 1L,
  seed = NULL,
  stability_seeds = NULL,
  stability_resampling = NULL,
  stability_ncores = ncores,
  return_fits = FALSE,
  top_n = 10L,
  epsilon = 1e-12,
  method = NULL,
  ...
)
}
\arguments{
\item{x}{Document-feature input for fitting. Accepted classes are
\link[Matrix:dgCMatrix-class]{dgCMatrix-class}, \link[quanteda:dfm]{dfm}, and
\code{DocumentTermMatrix}. A corpus is \strong{not} accepted; convert to a
document-feature matrix first with e.g. \code{\link[quanteda:dfm]{quanteda::dfm()}}.}

\item{engine}{Backend package. Forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}}.}

\item{model}{Model family. Forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}}.}

\item{k_grid}{Integer vector of topic counts \eqn{K} to evaluate. Defaults to
\code{5:15}.}

\item{metrics}{Character vector of metrics to compute for each candidate
\eqn{K}. Defaults to all eight metrics supported by
\code{\link[=evaluate_topic_model]{evaluate_topic_model()}}.}

\item{level}{Reporting level forwarded to \code{\link[=evaluate_topic_model]{evaluate_topic_model()}}. One of
\code{"aggregate"} (default), \code{"topic"}, or \code{"all"}.}

\item{control}{Named list of backend controls forwarded to
\code{\link[=fit_topic_model]{fit_topic_model()}} for every candidate \eqn{K}. Defaults to \code{list()}.}

\item{holdout}{Fraction of documents held out for \code{held_out_nll} and
\code{held_out_perplexity} metrics. Must be in \verb{[0, 1)}. Defaults to \code{0.2}.
When \code{holdout > 0}, the remaining fraction is used as \code{training} for
coherence and training likelihood metrics. When \code{holdout = 0}, coherence
and training likelihood metrics are computed on the full fitting input and
held-out metrics are marked unsupported because no held-out data is
available.
If none of \code{"held_out_nll"}, \code{"held_out_perplexity"}, \code{"train_nll"},
\code{"train_perplexity"}, \code{"coherence_npmi"}, or \code{"coherence_umass"} is in
\code{metrics}, the holdout split is skipped and the full \code{x} is used for
fitting.}

\item{ncores}{Number of parallel workers. Defaults to \code{1L} (sequential).
Each candidate \eqn{K} is fit independently, so parallelization scales
linearly with \code{length(k_grid)}. Uses \code{"PSOCK"} sockets; \code{"FORK"} is not used to
preserve quanteda/C++ stability.}

\item{seed}{Integer vector of length \code{length(k_grid)} used to seed each
candidate \eqn{K}'s fit reproducibly. If a single integer is supplied it is
expanded to a length-\code{length(k_grid)} vector starting from that value.
\code{NULL} means no seeding. Defaults to \code{NULL}.}

\item{stability_seeds}{Optional integer vector of seeds used to assess topic
stability for each candidate \eqn{K}. When \code{NULL} (default), no stability
runs are performed and output is unchanged. When supplied, each \eqn{K} is
refit across these seeds via \code{assess_topic_stability()}.}

\item{stability_resampling}{Optional resampling settings forwarded to
\code{assess_topic_stability()}. Defaults to \code{NULL}.}

\item{stability_ncores}{Integer. Number of workers used inside each
stability assessment when the outer K grid is sequential. Defaults to
\code{ncores}. Nested parallelism is avoided when \code{ncores > 1}.}

\item{return_fits}{Logical. Should the fitted models be returned as an
attribute of the result? Defaults to \code{FALSE}. Fits can be large; set
\code{TRUE} only when you need to inspect or reuse them.}

\item{top_n}{Integer. Forwarded to \code{\link[=evaluate_topic_model]{evaluate_topic_model()}}. Defaults to
\code{10L}.}

\item{epsilon}{Numeric. Forwarded to \code{\link[=evaluate_topic_model]{evaluate_topic_model()}}. Defaults to
\code{1e-12}.}

\item{method}{Fitting method forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}}. Defaults to
\code{NULL}.}

\item{...}{Additional arguments forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}}.}
}
\value{
An object of class \code{c("nlp_k_selection", "data.table")} with
columns:
\describe{
\item{\code{k}}{Topic count \eqn{K}.}
\item{\code{metric}}{Metric name.}
\item{\code{level}}{\code{"aggregate"} or \code{"topic"}.}
\item{\code{topic_id}}{\code{Topic###} for topic-level rows or \code{NA} for
aggregate rows. The column is retained even when only aggregate
metrics are requested so selection tables keep a stable long-format
schema.}
\item{\code{value}}{Numeric metric value.}
\item{\code{supported}}{Logical; \code{TRUE} when the metric was computed.}
}
If \code{return_fits = TRUE} the fitted models are stored in
\code{attr(result, "fits")}, a named list with names \code{"k<value>"}.
If \code{stability_seeds} is supplied, aggregate stability rows are added with
\code{metric = "stability"} and full per-topic stability outputs are stored in
\code{attr(result, "stability")}.
}
\description{
Fit a topic model for each value in a grid of topic counts and evaluate each
fit with \code{\link[=evaluate_topic_model]{evaluate_topic_model()}}. The result provides the information needed
to compare candidate values of \eqn{K} on multiple quality metrics
simultaneously.
}
\details{
\strong{Holdout split.} When \code{holdout > 0} and either predictive or coherence
metrics are requested, \code{x} is split at the document level into a training
shard (\code{1 - holdout} fraction) and a held-out shard (\code{holdout} fraction).
The split is random but reproducible when \code{seed} is supplied. The training
shard is passed to \code{\link[=fit_topic_model]{fit_topic_model()}} and to \code{\link[=evaluate_topic_model]{evaluate_topic_model()}} for
coherence and training likelihood metrics; the held-out shard is passed to
\code{\link[=evaluate_topic_model]{evaluate_topic_model()}} for held-out metrics. With \code{holdout = 0}, the full
\code{x} is used for fitting, coherence, and training likelihood metrics, while
held-out metrics are reported as unsupported.

A warning is issued when the number of documents is fewer than 50, because
the holdout shard may be too small for stable predictive metrics.

\strong{Parallelisation.} Uses \code{"PSOCK"} sockets. Each worker receives its own
\eqn{K} value and seed and runs the full fit + evaluate cycle independently.
The \code{ncores = 1} path bypasses cluster creation entirely and runs
sequentially.
}
\examples{
\dontshow{if (requireNamespace("text2vec", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 1, 1, 0,  0, 1, 2, 1,
             0, 0, 1, 2,  1, 0, 1, 1,  1, 2, 0, 1),
           nrow = 6, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:6)
colnames(dtm) <- paste0("term", 1:4)

sel <- select_k_topics(
  dtm, engine = "text2vec", model = "lda",
  k_grid  = 2:3,
  metrics = c("diversity", "exclusivity"),
  holdout = 0,
  seed    = 42L,
  control = list(fit = list(n_iter = 25, progressbar = FALSE))
)
print(sel)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=fit_topic_model]{fit_topic_model()}}, \code{\link[=evaluate_topic_model]{evaluate_topic_model()}},
\code{\link[=print.nlp_k_selection]{print.nlp_k_selection()}}, \code{\link[=plot.nlp_k_selection]{plot.nlp_k_selection()}}
}

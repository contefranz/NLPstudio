% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{fit_topic_model}
\alias{fit_topic_model}
\title{Fit a Topic Model Across Supported Backends}
\usage{
fit_topic_model(
  x,
  engine,
  model,
  k = NULL,
  method = NULL,
  data = NULL,
  return_dtw = TRUE,
  return_tww = TRUE,
  model_control = list(),
  fit_control = list(),
  dictionary = NULL,
  seedwords = NULL,
  initial_model = NULL
)
}
\arguments{
\item{x}{A document-feature input. Supported classes are
\link[Matrix:dgCMatrix-class]{dgCMatrix-class},
\link[quanteda:dfm]{dfm}, and
\code{DocumentTermMatrix}.}

\item{engine}{Backend package. One of \code{"text2vec"}, \code{"topicmodels"}, or
\code{"seededlda"}.}

\item{model}{Model family within the selected backend.
Supported combinations are:
\itemize{
\item \code{engine = "text2vec"} with \code{model = "lda"}
\item \code{engine = "topicmodels"} with \code{model = "lda"} or \code{"ctm"}
\item \code{engine = "seededlda"} with \code{model = "lda"}, \code{"seqlda"}, or \code{"seededlda"}
}}

\item{k}{Number of topics. Required for all supported models except
\verb{engine = "seededlda", model = "seededlda"}.}

\item{method}{Fitting method within the selected model family.
\itemize{
\item \code{topicmodels + lda}: \code{"VEM"} (default) or \code{"Gibbs"}
\item \code{topicmodels + ctm}: \code{"VEM"} only
\item \code{text2vec + lda}: \code{NULL} only
\item \code{seededlda}: \code{NULL} only
}}

\item{data}{Optional document metadata to store alongside the fitted model.
This can be a corpus, dfm, data.frame, or data.table with a document-ID
column. Explicit \code{data} is used by \code{get_dtw()} and
\code{get_representative_candidates()} in preference to metadata embedded in the
fitted backend object.}

\item{return_dtw}{Should DTW be cached in the returned object? Defaults to
\code{TRUE}.}

\item{return_tww}{Should TWW be cached in the returned object? Defaults to
\code{TRUE}.}

\item{model_control}{A named list routed to the model-construction stage.
In v0.6.0 this is used only for \code{engine = "text2vec"} and is forwarded to
\code{\link[text2vec:LatentDirichletAllocation]{LDA$new()}}.}

\item{fit_control}{A named list routed to the fitting stage.
\itemize{
\item \code{text2vec}: forwarded to \code{LDA$fit_transform()}
\item \code{topicmodels}: passed as \verb{control =}
\item \code{seededlda}: spliced into the selected \verb{textmodel_*()} call
}}

\item{dictionary}{Dictionary required for
\verb{engine = "seededlda", model = "seededlda"}.}

\item{seedwords}{Optional \code{seedwords} argument forwarded only to
\verb{engine = "topicmodels", model = "lda", method = "Gibbs"}.}

\item{initial_model}{Optional previously fitted model passed to backend
\verb{model =} arguments where supported.}
}
\value{
An S3 object of class \code{c("nlp_topic_fit", "list")}. It is a named
list with these fields:
\itemize{
\item \code{engine}: backend package used for estimation.
\item \code{model}: model family requested.
\item \code{method}: fitting method used, if applicable.
\item \code{model_object}: raw backend fit.
\item \code{dtw}: cached DTW in standardized \code{data.table} form, or \code{NULL}.
\item \code{tww}: cached TWW in standardized \code{data.table} form, or \code{NULL}.
\item \code{data}: stored metadata supplied at fit time, or \code{NULL}.
\item \code{call}: matched function call.
}

Users access these components with \code{$}, for example \code{fit$dtw} or
\code{fit$model_object}.
}
\description{
Fit a topic model with a unified API across \strong{text2vec}, \strong{topicmodels},
and \strong{seededlda}. The fitted object stores both the raw backend fit and, by
default, cached DTW/TWW outputs following Lewis and Grossetti (2022):
}
\details{
\itemize{
\item \strong{DTW}: document-topic weights
\item \strong{TWW}: topic-word weights
}

\code{fit_topic_model()} standardizes model fitting while preserving the original
backend object in \code{model_object}. That design avoids brittle inheritance
across R6, S4, and list-based classes while still providing a stable package
interface for downstream helpers such as \code{get_dtw()}, \code{get_tww()},
\code{\link[=get_top_terms]{get_top_terms()}}, and \code{\link[=plot_dtw]{plot_dtw()}}.

The standardized DTW/TWW outputs always use topic identifiers of the form
\code{Topic001}, \code{Topic002}, and so on, regardless of backend-specific naming.

The API currently covers these model families and fitting algorithms:
\itemize{
\item LDA via \strong{text2vec}, \strong{topicmodels}, and \strong{seededlda}
\item CTM via \strong{topicmodels}
\item WarpLDA as the \strong{text2vec} estimation algorithm for LDA
\item Sequential LDA via \strong{seededlda}
\item Seeded LDA via \strong{seededlda}
}
}
\examples{
dtm <- methods::as(
  Matrix::Matrix(
    matrix(
      c(1, 0, 1,
        1, 1, 0,
        0, 1, 1,
        1, 1, 1),
      nrow = 4,
      byrow = TRUE
    ),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:4)
colnames(dtm) <- paste0("term", 1:3)

fit <- fit_topic_model(
  dtm,
  engine = "text2vec",
  model = "lda",
  k = 2,
  model_control = list(doc_topic_prior = 0.1, topic_word_prior = 0.01),
  fit_control = list(n_iter = 25, progressbar = FALSE)
)

class(fit)
names(fit)

if (requireNamespace("topicmodels", quietly = TRUE)) {
  fit_topic_model(
    dtm,
    engine = "topicmodels",
    model = "lda",
    k = 2,
    fit_control = list(seed = 1, em = list(iter.max = 5), var = list(iter.max = 5))
  )

  fit_topic_model(
    dtm,
    engine = "topicmodels",
    model = "ctm",
    k = 2,
    fit_control = list(seed = 1, em = list(iter.max = 5), var = list(iter.max = 5))
  )
}

if (requireNamespace("seededlda", quietly = TRUE)) {
  fit_topic_model(
    dtm,
    engine = "seededlda",
    model = "lda",
    k = 2,
    fit_control = list(max_iter = 100, verbose = FALSE)
  )

  suppressWarnings(
    fit_topic_model(
      dtm,
      engine = "seededlda",
      model = "seqlda",
      k = 2,
      fit_control = list(max_iter = 100, verbose = FALSE)
    )
  )

  dict <- quanteda::dictionary(list(
    topic_a = c("term1", "term2"),
    topic_b = c("term3")
  ))

  fit_topic_model(
    dtm,
    engine = "seededlda",
    model = "seededlda",
    dictionary = dict,
    fit_control = list(max_iter = 100, verbose = FALSE)
  )
}

}
\references{
Lewis, C. M., & Grossetti, F. (2022).
\href{https://www.jmlr.org/papers/volume23/19-297/19-297.pdf}{A statistical approach for optimal topic model identification}.
\emph{Journal of Machine Learning Research}, 23(58), 1-20.

Blei, D. M., Ng, A. Y., & Jordan, M. I. (2003).
\href{https://www.jmlr.org/papers/volume3/blei03a/blei03a.pdf}{Latent Dirichlet Allocation}.
\emph{Journal of Machine Learning Research}, 3, 993-1022.

Blei, D. M., & Lafferty, J. D. (2006).
\href{https://www.cs.cmu.edu/afs/cs/usr/lafferty/www/pub/ctm.pdf}{Correlated topic models}.
\emph{Advances in Neural Information Processing Systems}, 18, 147.

Blei, D. M., & Lafferty, J. D. (2007).
\href{https://doi.org/10.1214/07-AOAS114}{A correlated topic model of Science}.
\emph{The Annals of Applied Statistics}, 1(1), 17-35.

Chen, J., Li, K., Zhu, J., & Chen, W. (2016).
\href{https://pacman.cs.tsinghua.edu.cn/~cwg/publication/vldb16/vldb16.pdf}{WarpLDA: A Cache Efficient O(1) Algorithm for Latent Dirichlet Allocation}.
\emph{Proceedings of the VLDB Endowment}, 9(10), 744-755.

Du, L., Buntine, W. L., Jin, H., & Chen, C. (2012).
\href{https://doi.org/10.1007/s10115-011-0425-1}{Sequential latent Dirichlet allocation}.
\emph{Knowledge and Information Systems}, 31(3), 475-503.

Lu, B., Ott, M., Cardie, C., & Tsou, B. K. (2011).
\href{https://doi.org/10.1109/ICDMW.2011.125}{Multi-aspect sentiment analysis with topic models}.
In \emph{2011 IEEE 11th International Conference on Data Mining Workshops}, 81-88.

Jagarlamudi, J., Daume III, H., & Udupa, R. (2012).
\href{https://aclanthology.org/E12-1021.pdf}{Incorporating lexical priors into topic models}.
In \emph{Proceedings of the 13th Conference of the European Chapter of the Association for Computational Linguistics}, 204-213.

Watanabe, K., & Zhou, Y. (2022).
\href{https://journals.sagepub.com/doi/full/10.1177/0894439320907027}{Theory-Driven Analysis of Large Corpora: Semisupervised Topic Classification of the UN Speeches}.
\emph{Social Science Computer Review}, 40(2), 346-366.

Watanabe, K., & Baturo, A. (2024).
\href{https://journals.sagepub.com/doi/10.1177/08944393231178605}{Seeded Sequential LDA: A Semi-Supervised Algorithm for Topic-Specific Analysis of Sentences}.
\emph{Social Science Computer Review}, 42(1), 224-248.
}

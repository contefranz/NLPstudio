% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_diagnostics.R
\name{summarize_topics}
\alias{summarize_topics}
\title{Summarize Topics for Interpretation}
\usage{
summarize_topics(
  fit,
  training = NULL,
  doc_data = NULL,
  top_n = 10L,
  representative_n = 3L,
  include_text = FALSE,
  docvars = FALSE,
  doc_id_col = "doc_id",
  text_col = "text"
)
}
\arguments{
\item{fit}{An \code{nlp_topic_fit} object returned by \code{\link[=fit_topic_model]{fit_topic_model()}}.}

\item{training}{Optional training document-feature matrix. When supplied,
coherence metrics are included.}

\item{doc_data}{Optional document metadata or text source forwarded to
\code{\link[=get_dtw]{get_dtw()}} and \code{\link[=get_representative_candidates]{get_representative_candidates()}}.}

\item{top_n}{Integer. Number of top terms per topic. Defaults to \code{10L}.}

\item{representative_n}{Integer. Number of representative documents to
retain per topic. Defaults to \code{3L}.}

\item{include_text}{Logical. Should representative text be included when
available? Defaults to \code{FALSE}.}

\item{docvars}{Logical. Should stored document variables be available for
representative selection output? Defaults to \code{FALSE}.}

\item{doc_id_col}{Document-ID column name when \code{doc_data} is tabular.
Defaults to \code{"doc_id"}.}

\item{text_col}{Text column name when \code{doc_data} is tabular. Defaults to
\code{"text"}.}
}
\value{
A \link[data.table:data.table]{data.table} with one row per topic.
}
\description{
Build a compact one-row-per-topic interpretation table from an
\code{nlp_topic_fit}. The table combines top terms, prevalence, available
evaluation metrics, and representative documents.
}
\examples{
\dontshow{if (requireNamespace("topicmodels", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 1, 1, 0,  0, 1, 2, 1,
             0, 0, 1, 2,  1, 0, 1, 1,  1, 2, 0, 1),
           nrow = 6, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:6)
colnames(dtm) <- paste0("term", 1:4)
fit <- fit_topic_model(
  dtm,
  engine = "topicmodels",
  model = "lda",
  k = 2,
  method = "Gibbs",
  control = list(fit = list(seed = 1, iter = 50, burnin = 0, thin = 1))
)
summarize_topics(fit, training = dtm, top_n = 3)
\dontshow{\}) # examplesIf}
}

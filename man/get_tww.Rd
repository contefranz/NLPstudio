% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{get_tww}
\alias{get_tww}
\title{Extract Standardized Topic Word Weights}
\usage{
get_tww(x)
}
\arguments{
\item{x}{A supported topic-model object. This includes \code{nlp_topic_fit},
\code{warp_lda()} output, raw \code{topicmodels} fits, raw \code{seededlda} fits, raw
\code{text2vec::LDA} objects, and already standardized TWW tables.}
}
\value{
A \code{data.table} with one row per topic, a \code{topic_id} column using
the \code{Topic###} convention, and one column per term.
}
\description{
Extract TWW (topic-word weights) from a supported topic-model object and
return a standardized wide \link[data.table:data.table]{data.table}.
}
\examples{
dtm <- methods::as(
  Matrix::Matrix(
    matrix(
      c(1, 0, 1,
        1, 1, 0,
        0, 1, 1,
        1, 1, 1),
      nrow = 4,
      byrow = TRUE
    ),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:4)
colnames(dtm) <- paste0("term", 1:3)

fit <- fit_topic_model(
  dtm,
  engine = "text2vec",
  model = "lda",
  k = 2,
  fit_control = list(n_iter = 25, progressbar = FALSE)
)

get_tww(fit)

}

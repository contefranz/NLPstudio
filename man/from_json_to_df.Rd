% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/from_json_to_df.R
\name{from_json_to_df}
\alias{from_json_to_df}
\title{Convert JSON to data.table}
\usage{
from_json_to_df(
  files,
  ncores = 1,
  nchunks = ncores,
  socket = c("PSOCK", "FORK"),
  drop_late_filers = FALSE,
  what = NULL,
  drop_empty_text = TRUE,
  max_chunk_size = NULL,
  ...
)
}
\arguments{
\item{files}{Character vector of JSON file paths.}

\item{ncores}{Integer. Number of CPU cores to use for parallel
processing. Defaults to 1 (sequential).}

\item{nchunks}{Integer. Number of chunks to split the input file vector into.
Defaults to \code{ncores}. The chunk size is computed as
\code{ceiling(length(files) / nchunks)}. Ignored if \code{max_chunk_size} is
explicitly provided via \code{...}.}

\item{socket}{Character. Parallel backend to use. One of \code{"PSOCK"}
(default, recommended) or \code{"FORK"}. On Windows, \code{"FORK"} is not
supported and will trigger an error.}

\item{drop_late_filers}{Logical. If \code{TRUE}, removes filings considered
"late" (filing year greater than fiscal year + 1). Default \code{FALSE}.}

\item{what}{Character or \code{NULL}. Recommended explicit selector for the JSON
family being imported. Supported values are \code{"10-K"}, \code{"10-Q"}, \code{"8-K"},
and \code{"loan"}. When \code{NULL} (default), the function infers the family from
the JSON keys for backward compatibility.}

\item{drop_empty_text}{Logical. If \code{TRUE} (default), drop rows where the
extracted section text is empty or missing after melting.}

\item{max_chunk_size}{Integer. If provided, sets the exact number of files
per chunk, overriding the value derived from \code{nchunks}. Use this for
fine-grained memory control when file sizes vary significantly.}

\item{...}{Additional arguments passed to internal processing steps.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with one row per \verb{(document × item)}
and columns:
\itemize{
\item \code{cik}: Central Index Key (integer)
\item \code{filing_date}, \code{period_of_report} (IDate)
\item \code{fyear}: fiscal year (integer)
\item \code{sic}: industry code (integer)
\item \code{item}: item identifier (character)
\item \code{text}: filing text content (character)
\item plus any additional metadata extracted upstream
}
}
\description{
Converts a vector of JSON file paths into a unified \link[data.table:data.table]{data.table::data.table}
suitable for downstream analysis. The function is optimized for large-scale
input (thousands of JSON files) and leverages both chunking and user-selected
parallel backends to remain efficient and memory-safe.
}
\details{
Internally the function proceeds in three phases:
\enumerate{
\item \strong{Read & parse} – The input file paths are divided into chunks. By
default, chunk size is derived from \code{nchunks} as
\code{ceiling(length(files) / nchunks)}. If \code{max_chunk_size} is explicitly
provided, it overrides this calculation. Each chunk is read in parallel
with \code{\link[RcppSimdJson:fparse]{RcppSimdJson::fload()}} and converted to data tables.
\item \strong{Reshape} – Each parsed table is normalized and reshaped from wide to
long format in parallel. The selected text columns depend on \code{what}:
\code{"10-K"} uses \verb{item_*}/\verb{section_*}, \code{"10-Q"} uses \verb{part_*} and
\verb{part_*_item_*}, \code{"8-K"} uses \verb{item_*}, and \code{"loan"} uses the canonical
section names produced by \code{sec-crawler}. When \code{what = NULL}, the function
infers the family from the JSON keys for backward compatibility.
\item \strong{Combine & clean} – Melted tables are bound together, date columns
converted, fiscal year (\code{fyear}) derived, late filers optionally dropped,
and column order standardized.
}

Parallel backends are controlled with the \code{socket} argument:
\itemize{
\item When \code{socket = "PSOCK"}, \code{\link[parallel:clusterApply]{parallel::clusterApplyLB()}} is used, which
dynamically balances work across workers and is portable across operating
systems.
\item When \code{socket = "FORK"}, \code{\link[parallel:mclapply]{parallel::mclapply()}} is used, which can be
faster on Linux/macOS because it avoids copying large objects to workers.
}
}
\section{Chunking strategy}{

The function offers two ways to control chunking:
\itemize{
\item \strong{\code{nchunks}} (default): Set the number of batches. With 7000 files
and \code{nchunks = 4}, each batch contains ~1750 files.
\item \strong{\code{max_chunk_size}} (explicit): Set the exact batch size. With 7000
files and \code{max_chunk_size = 500}, you get 14 batches of 500 files each.
}
If both are relevant, \code{max_chunk_size} takes precedence.
}

\section{Efficiency considerations}{

\itemize{
\item \strong{RcppSimdJson} is used for parsing, which is significantly faster than
base or jsonlite parsers on large files.
\item Chunking is controlled by \code{nchunks} for load balancing, while \code{max_chunk_size}
provides an upper bound on files per chunk to prevent memory overload.
\item \code{socket = "FORK"} is generally preferred on Linux/macOS for speed, while
\code{socket = "PSOCK"} is more portable and provides dynamic load balancing.
}
}

\seealso{
\code{\link[RcppSimdJson:fparse]{RcppSimdJson::fload()}}, \code{\link[data.table:melt.data.table]{data.table::melt()}},
\code{\link[parallel:mclapply]{parallel::mclapply()}}, \code{\link[parallel:clusterApply]{parallel::clusterApplyLB()}}
}

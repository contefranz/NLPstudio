% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optop_bridge.R
\name{as_optop_input}
\alias{as_optop_input}
\title{Prepare NLPstudio Topic Models for OpTop}
\usage{
as_optop_input(x, weighted_dfm)
}
\arguments{
\item{x}{An \code{nlp_k_selection} object created with \code{return_fits = TRUE}, a
list of \code{nlp_topic_fit} objects, or a list of raw \code{LDA_VEM} objects.}

\item{weighted_dfm}{A weighted \link[quanteda:dfm]{quanteda::dfm}, usually created with
\code{\link[=as_optop_weighted_dfm]{as_optop_weighted_dfm()}} from the same fitting input used for the LDA
models.}
}
\value{
A list of class \code{c("nlp_optop_input", "list")} with:
\describe{
\item{\code{lda_models}}{Raw \code{LDA_VEM} objects ordered by topic count.}
\item{\code{weighted_dfm}}{Weighted DFM aligned to the LDA vocabulary.}
\item{\code{k}}{Integer topic counts in ascending order.}
}
}
\description{
Extract an ordered list of raw \code{topicmodels::LDA(method = "VEM")} fits and a
vocabulary-aligned weighted DFM for \code{OpTop::optimal_topic()}.
}
\details{
OpTop currently expects \code{LDA_VEM} objects from \strong{topicmodels}. This adapter
intentionally rejects Gibbs LDA, CTM, \strong{text2vec}, \strong{seededlda}, ETM, and
partial fits so that users do not pass objects outside OpTop's current
assumptions.

NLPstudio does not import or call \strong{OpTop}. After preparing the input, call
\code{OpTop::optimal_topic(lda_models = input$lda_models, weighted_dfm = input$weighted_dfm, ...)} when \strong{OpTop} is installed.
}
\examples{
\dontshow{if (requireNamespace("topicmodels", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 1, 1, 0,  0, 1, 2, 1,
             0, 0, 1, 2,  1, 0, 1, 1,  1, 2, 0, 1),
           nrow = 6, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:6)
colnames(dtm) <- paste0("term", 1:4)
dfmat <- quanteda::as.dfm(dtm)

selection <- select_k_topics(
  dfmat,
  engine = "topicmodels",
  model = "lda",
  method = "VEM",
  k_grid = 2:3,
  metrics = c("diversity", "exclusivity"),
  holdout = 0,
  return_fits = TRUE,
  control = list(fit = list(seed = 1, em = list(iter.max = 5), var = list(iter.max = 5)))
)

optop_input <- as_optop_input(selection, as_optop_weighted_dfm(dfmat))
# OpTop::optimal_topic(optop_input$lda_models, optop_input$weighted_dfm)
\dontshow{\}) # examplesIf}
}

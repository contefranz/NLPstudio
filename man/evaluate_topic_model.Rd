% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_topic_model.R
\name{evaluate_topic_model}
\alias{evaluate_topic_model}
\title{Evaluate a Fitted Topic Model}
\usage{
evaluate_topic_model(
  fit,
  training = NULL,
  newdata = NULL,
  metrics = c("coherence_npmi", "coherence_umass", "diversity", "exclusivity",
    "held_out_nll", "held_out_perplexity", "train_nll", "train_perplexity"),
  top_n = 10L,
  epsilon = 1e-12
)
}
\arguments{
\item{fit}{An object of class \code{nlp_topic_fit} returned by \code{\link[=fit_topic_model]{fit_topic_model()}}.}

\item{training}{The document-feature matrix used to train \code{fit}. Required for
coherence metrics (\code{"coherence_npmi"}, \code{"coherence_umass"}) and training
likelihood metrics (\code{"train_nll"}, \code{"train_perplexity"}). Accepted classes
are \link[Matrix:dgCMatrix-class]{dgCMatrix-class}, \link[quanteda:dfm]{dfm}, and
\code{DocumentTermMatrix}. Defaults to \code{NULL}.}

\item{newdata}{A held-out document-feature matrix. Required for predictive
metrics (\code{"held_out_nll"}, \code{"held_out_perplexity"}). Accepted classes are
the same as \code{training}. Defaults to \code{NULL}.}

\item{metrics}{Character vector of metrics to compute. Defaults to all eight
supported metrics (alphabetical):
\describe{
\item{\code{"coherence_npmi"}}{Normalized Pointwise Mutual Information
coherence per topic (Aletras & Stevenson, 2013). Requires \code{training}.}
\item{\code{"coherence_umass"}}{UMass coherence per topic
(Mimno et al., 2011). Requires \code{training}.}
\item{\code{"diversity"}}{Proportion of unique top-N terms across all topics.
Engine-agnostic; no extra data required.}
\item{\code{"exclusivity"}}{STM-style per-topic exclusivity: mean share of
each top-N term's probability belonging to that topic. Engine-agnostic;
no extra data required.}
\item{\code{"held_out_nll"}}{Mean negative log-likelihood per token on
\code{newdata}. Requires \code{newdata}.}
\item{\code{"held_out_perplexity"}}{Held-out perplexity. Equal to
\code{exp(held_out_nll)}. Requires \code{newdata}.}
\item{\code{"train_nll"}}{Mean negative log-likelihood per token on
\code{training}. Requires \code{training}.}
\item{\code{"train_perplexity"}}{Training perplexity. Equal to
\code{exp(train_nll)}. Requires \code{training}.}
}}

\item{top_n}{Integer. Number of top terms per topic used by coherence,
diversity, and exclusivity. Defaults to \code{10L}.}

\item{epsilon}{Small positive constant for numerical stability in logarithm
computations. Defaults to \code{1e-12}.}
}
\value{
A \link[data.table:data.table]{data.table} with columns:
\describe{
\item{\code{metric}}{Metric name (one of the values in \code{metrics}).}
\item{\code{scope}}{\code{"overall"} for corpus-level scalars, \code{"per_topic"} for
topic-level values.}
\item{\code{topic_id}}{\code{Topic###} identifier for \code{"per_topic"} rows;
\code{NA} for \code{"overall"} rows.}
\item{\code{value}}{Numeric metric value. \code{NA} when \code{supported = FALSE}.}
\item{\code{supported}}{\code{TRUE} when the metric was computed; \code{FALSE} when
the required data is missing or the metric is unsupported for the
given engine.}
}
Rows are ordered by \code{metric} then \code{scope} then \code{topic_id}.
}
\description{
Compute a standardized set of quality metrics for an object returned by
\code{\link[=fit_topic_model]{fit_topic_model()}}. Metrics are returned in a single long-format
\link[data.table:data.table]{data.table} so that results from different
engines and different values of k can be compared directly.
}
\details{
\strong{Coherence} metrics require \code{training} to be the same corpus used to fit
the model. They are computed in-package using sparse co-occurrence statistics
from \code{training}, so results are directly comparable across all supported
engines. Terms in \code{fit$vocab} that are absent from \code{training} contribute
zero to all co-occurrence counts.

\strong{Diversity} is the proportion of unique terms among all available
top-\code{top_n} terms across topics:
\code{length(unique_top_terms) / (k * min(top_n, vocabulary_size))}. A value of
1 means no term appears in more than one topic's top list; a low value
indicates topics that share high-probability terms.

\strong{Exclusivity} (per topic \code{t}) is the mean, over the top-\code{top_n} terms of
\code{t}, of \verb{phi[t, w] / sum_j phi[j, w]}. High exclusivity means those terms
are concentrated in that topic rather than spread across topics.

\strong{Training and held-out likelihood metrics} align the supplied corpus to the
fitted vocabulary, obtain document-topic weights, then combine those weights
with \code{fit$tww} to reconstruct per-token log-likelihoods. Training metrics use
cached fitted document-topic weights when available; held-out metrics infer
document-topic weights for \code{newdata} with the fitted topic-word weights held
fixed. Documents whose terms are all outside the fitted vocabulary are
dropped with a warning (they carry no information under the fitted model).
Tokens outside the fitted vocabulary are excluded from the token count,
matching the convention used by \code{topicmodels::perplexity()}.
}
\examples{
\dontshow{if (requireNamespace("text2vec", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 1, 1, 0,  0, 1, 2, 1,  0, 0, 1, 2),
           nrow = 4, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:4)
colnames(dtm) <- paste0("term", 1:4)

fit <- fit_topic_model(
  dtm, engine = "text2vec", model = "lda", k = 2,
  control = list(fit = list(n_iter = 25, progressbar = FALSE))
)

# Engine-agnostic metrics only (no extra data needed)
evaluate_topic_model(fit, metrics = c("diversity", "exclusivity"))

# Coherence and training likelihood with training data
evaluate_topic_model(fit, training = dtm,
                     metrics = c("coherence_npmi", "train_perplexity"))
\dontshow{\}) # examplesIf}
}
\references{
Aletras, N., & Stevenson, M. (2013). Evaluating topic coherence using
distributional semantics. \emph{EACL}, 13-22.

Mimno, D., Wallach, H., Talley, E., Leenders, M., & McCallum, A. (2011).
Optimizing semantic coherence in topic models. \emph{EMNLP}, 262-272.

Roberts, M. E., Stewart, B. M., Tingley, D., Lucas, C., Leder-Luis, J.,
Gadarian, S. K., Albertson, B., & Rand, D. G. (2014). Structural topic
models for open-ended survey responses. \emph{American Journal of Political
Science}, 58(4), 1064-1082.
}
\seealso{
\code{\link[=fit_topic_model]{fit_topic_model()}}, \code{\link[=predict_topic_model]{predict_topic_model()}}, \code{\link[=select_k_topics]{select_k_topics()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{get_topic_hyperparameters}
\alias{get_topic_hyperparameters}
\title{Extract Topic-Model Hyperparameters}
\usage{
get_topic_hyperparameters(x)
}
\arguments{
\item{x}{An object returned by \code{\link[=fit_topic_model]{fit_topic_model()}}.}
}
\value{
A \link[data.table:data.table]{data.table} with columns:
\describe{
\item{\code{parameter}}{Standardized parameter name: \code{"k"}, \code{"alpha"}, or
\code{"beta"}.}
\item{\code{value}}{Stored parameter value. Scalar symmetric priors are shown
as scalars; non-scalar priors are preserved as vectors. \code{NA} means the
parameter is not available or not applicable for the fitted model.}
\item{\code{source_section}}{Where the value came from: e.g. \code{"argument"},
\code{"model"}, \code{"fit"}, or \code{"model_object"}.}
\item{\code{source_name}}{Backend-native argument, slot, or field name.}
}
}
\description{
Return standardized hyperparameters stored on an object returned by
\code{\link[=fit_topic_model]{fit_topic_model()}}. The accessor uses package-level names so users do not
need to remember backend-specific argument names.
}
\details{
\code{alpha} is the document-topic prior and \code{beta} is the topic-word prior, using
the notation common in LDA. Engines that do not expose an equivalent prior
return \code{NA} for that row instead of dropping it, so the table has a stable
shape across engines. Objects created before hyperparameters were stored on
\code{nlp_topic_fit} objects return a best-effort fallback with a warning; refit
the model with the current package version to recover backend-native
hyperparameter sources.
}
\examples{
\dontshow{if (requireNamespace("text2vec", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(1, 0, 1,  1, 1, 0,  0, 1, 1,  1, 1, 1),
           nrow = 4, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
colnames(dtm) <- paste0("term", 1:3)
fit <- fit_topic_model(
  dtm, engine = "text2vec", model = "lda", k = 2,
  control = list(
    model = list(doc_topic_prior = 0.1, topic_word_prior = 0.01),
    fit = list(n_iter = 25, progressbar = FALSE)
  )
)
get_topic_hyperparameters(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=fit_topic_model]{fit_topic_model()}}
}

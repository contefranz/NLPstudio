% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_diagnostics.R
\name{assess_topic_stability}
\alias{assess_topic_stability}
\title{Assess Topic Stability Across Repeated Fits}
\usage{
assess_topic_stability(
  x,
  engine = NULL,
  model = NULL,
  k = NULL,
  seeds = NULL,
  method = NULL,
  control = list(),
  resampling = NULL,
  ncores = 1L,
  return_fits = FALSE,
  ...
)
}
\arguments{
\item{x}{Either a document-feature input accepted by \code{\link[=fit_topic_model]{fit_topic_model()}}, or
a list of pre-fitted \code{nlp_topic_fit} objects.}

\item{engine}{Backend package forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}} when \code{x} is
model input.}

\item{model}{Model family forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}} when \code{x} is model
input.}

\item{k}{Number of topics forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}} when \code{x} is model
input.}

\item{seeds}{Integer vector of seeds. In repeated-fit mode this is required
and must contain at least two unique integer seeds. In list-of-fits mode it
is optional and, when supplied, must match the number of fits.}

\item{method}{Fitting method forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}}.}

\item{control}{Backend controls forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}}. For
\code{engine = "topicmodels"}, each seed is also written to
\code{control$fit$seed} before fitting so backend-native seeding is explicit.}

\item{resampling}{Optional list with \code{fraction}, a number in \verb{(0, 1]}. When
supplied, each seed also draws that fraction of documents without
replacement before fitting. Defaults to \code{NULL}, which means no resampling.}

\item{ncores}{Integer. Number of PSOCK workers for repeated fitting.
Defaults to \code{1L}.}

\item{return_fits}{Logical. Should fitted models be attached as
\code{attr(result, "fits")}? Defaults to \code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{\link[=fit_topic_model]{fit_topic_model()}} in
repeated-fit mode.}
}
\value{
An object of class \code{c("nlp_topic_stability", "data.table")} with one
row per reference-topic/run comparison. Topics from all non-reference runs
are matched to the first run. Columns include run metadata, matched topic
IDs, cosine similarity, per-topic stability, per-run stability, aggregate
stability, and model metadata.
}
\description{
Repeatedly fit the same topic-model specification across multiple seeds and
compare the resulting topics after label matching. The function is a
transparent wrapper around \code{\link[=fit_topic_model]{fit_topic_model()}}: unless \code{resampling} is
supplied, each run uses the same data, backend, model, topic count, method,
controls, and additional arguments, changing only the random seed.
}
\details{
Topic labels are arbitrary across model runs. \code{assess_topic_stability()}
therefore extracts standardized topic-word weights with \code{\link[=get_tww]{get_tww()}}, aligns
vocabularies, matches topics from each run to the first run, and reports
matched-topic cosine similarities.
}
\examples{
\dontshow{if (requireNamespace("topicmodels", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 1, 1, 0,  0, 1, 2, 1,
             0, 0, 1, 2,  1, 0, 1, 1,  1, 2, 0, 1),
           nrow = 6, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:6)
colnames(dtm) <- paste0("term", 1:4)

assess_topic_stability(
  dtm,
  engine = "topicmodels",
  model = "lda",
  k = 2,
  method = "Gibbs",
  seeds = 1:2,
  control = list(fit = list(iter = 50, burnin = 0, thin = 1))
)

fits <- lapply(1:2, function(s) {
  fit_topic_model(
    dtm,
    engine = "topicmodels",
    model = "lda",
    k = 2,
    method = "Gibbs",
    control = list(fit = list(seed = s, iter = 50, burnin = 0, thin = 1))
  )
})
assess_topic_stability(fits, seeds = 1:2)
\dontshow{\}) # examplesIf}
}

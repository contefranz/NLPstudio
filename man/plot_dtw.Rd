% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dtw.R
\name{plot_dtw}
\alias{plot_dtw}
\title{Plot Distribution of Document-Topic-Weights}
\usage{
plot_dtw(theta, facet_args = list(scales = "free_y"), ...)
}
\arguments{
\item{theta}{A \code{data.table} of document-topic proportions, with one row per document and one
column per topic. The first column must be \code{doc_id}, and all other columns should contain
numeric topic weights.}

\item{facet_args}{A named list of additional arguments passed to \code{\link[=facet_wrap]{facet_wrap()}},
such as \code{ncol}, \code{nrow}, or \code{strip.position}. By default, \code{scales = "free_y"}
is used to allow individual y-axis scaling for each topic.}

\item{...}{Additional arguments passed to \code{\link[=geom_histogram]{geom_histogram()}},
such as \code{binwidth}, \code{fill}, or \code{color}.}
}
\value{
A \code{ggplot} object representing the faceted histogram of topic proportions.
}
\description{
\code{plot_dtw()} visualizes the distribution of topic proportions across documents
from a \code{theta} matrix, typically returned by a topic model such as \code{\link[=warpLDA]{warpLDA()}}.
Each topic is shown as a separate histogram, allowing users to assess the sparsity,
dominance, or spread of each topic across the corpus.
}
\details{
Internally, the function reshapes the \code{theta} matrix to long format and constructs a faceted
histogram using \link{ggplot2}. Each facet corresponds to a topic and shows the density of the topic
proportions across all documents. This is useful for diagnosing topic quality, sparsity, and
prevalence.

The \code{theta} matrix is expected to follow the structure returned by \code{\link[=warpLDA]{warpLDA()}}, where \code{doc_id}
is the document identifier and remaining columns represent topic probabilities
(\code{Topic001}, \code{Topic002}, etc.).

If one ensures the presence of the column \code{doc_id}, then \code{plot_dtw()} can be used on a
\link[topicmodels:tm-class]{TopicModel-class} class object from the package \strong{topicmodels}.
}
\seealso{
\code{\link[=warpLDA]{warpLDA()}}, \code{\link[topicmodels:lda]{topicmodels::LDA()}}, \code{\link[=geom_histogram]{geom_histogram()}}, \code{\link[=facet_wrap]{facet_wrap()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ngram_tokens.R
\name{ngram_tokens}
\alias{ngram_tokens}
\title{Construct Token N-Grams}
\usage{
ngram_tokens(
  x,
  n = 2L,
  skip = 0L,
  concatenator = "_",
  ncores = 1,
  nchunks = ncores,
  socket = c("PSOCK", "FORK")
)
}
\arguments{
\item{x}{A \link[quanteda:tokens]{quanteda::tokens} object.}

\item{n}{Integer vector. The number(s) of tokens to concatenate. For example
\code{n = 2} produces bigrams and \code{n = 1:2} keeps both unigrams and bigrams.}

\item{skip}{Integer vector. The number of tokens to skip when forming
n-grams. \code{skip = 0} (default) yields adjacent n-grams; positive values
yield skip-grams. See \code{\link[quanteda:tokens_ngrams]{quanteda::tokens_ngrams()}}.}

\item{concatenator}{Character used to join tokens within an n-gram. Defaults
to \code{"_"}.}

\item{ncores}{Integer. Number of CPU cores to use for parallel
processing. Defaults to 1 (sequential).}

\item{nchunks}{Integer. Number of chunks to split the corpus into.
Defaults to \code{ncores}. Setting \code{nchunks > ncores} can improve load
balancing when documents vary in size. See \emph{Details}.}

\item{socket}{Character. Parallel backend to use. One of \code{"PSOCK"}
(default, recommended) or \code{"FORK"}. On Windows, \code{"FORK"} is not
supported and will trigger an error.}
}
\value{
A \link[quanteda:tokens]{quanteda::tokens} object containing the requested n-grams, with
documents in the same order as the input.
}
\description{
Build n-grams (and optional skip-grams) from a \strong{quanteda} \link[quanteda:tokens]{quanteda::tokens}
object in parallel, keeping the workflow inside the NLPstudio API. This is a
thin, parallel-aware wrapper around \code{\link[quanteda:tokens_ngrams]{quanteda::tokens_ngrams()}}.
}
\details{
The tokens object is split into balanced chunks by document and processed with
the package's shared parallel backend. See \code{\link[=tokenize_corpus]{tokenize_corpus()}} for the
chunking strategy and backend details.
}
\examples{
corp <- quanteda::corpus(c(
  doc1 = "the quick brown fox",
  doc2 = "a slow green turtle"
))
toks <- tokenize_corpus(corp)
ngram_tokens(toks, n = 2)

}
\seealso{
\code{\link[=compound_tokens]{compound_tokens()}}, \code{\link[=detect_collocations]{detect_collocations()}}
}

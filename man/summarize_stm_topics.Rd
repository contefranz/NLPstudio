% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm_interpretation.R
\name{summarize_stm_topics}
\alias{summarize_stm_topics}
\title{Summarize STM Topics}
\usage{
summarize_stm_topics(
  fit,
  training = NULL,
  doc_data = NULL,
  top_n = 10L,
  representative_n = 3L,
  include_text = FALSE,
  docvars = FALSE,
  label_n = 7L,
  label_types = c("prob", "frex", "lift", "score"),
  frexweight = 0.5,
  include_sage = FALSE,
  doc_id_col = "doc_id",
  text_col = "text"
)
}
\arguments{
\item{fit}{An STM \code{nlp_topic_fit} returned by \code{\link[=fit_topic_model]{fit_topic_model()}} or a raw
\strong{stm} \code{STM} object without content covariates.}

\item{training}{Optional training document-feature matrix forwarded to
\code{\link[=summarize_topics]{summarize_topics()}}.}

\item{doc_data}{Optional document metadata or text source forwarded to
\code{\link[=summarize_topics]{summarize_topics()}}.}

\item{top_n}{Integer. Number of probability top terms used by the generic
topic summary. Defaults to \code{10L}.}

\item{representative_n}{Integer. Number of representative documents retained
per topic. Defaults to \code{3L}.}

\item{include_text}{Logical. Should representative text be included when
available? Defaults to \code{FALSE}.}

\item{docvars}{Logical. Should stored document variables be available for
representative selection output? Defaults to \code{FALSE}.}

\item{label_n}{Integer. Number of STM-native label terms per label type.
Defaults to \code{7L}.}

\item{label_types}{Character vector of STM label families. Valid values are
\code{"prob"}, \code{"frex"}, \code{"lift"}, and \code{"score"}.}

\item{frexweight}{Numeric value in \verb{[0, 1]} forwarded to
\code{\link[stm:labelTopics]{stm::labelTopics()}}. Defaults to \code{0.5}.}

\item{include_sage}{Logical. Should SAGE marginal label columns be included?
Defaults to \code{FALSE}.}

\item{doc_id_col}{Document-ID column name when \code{doc_data} is tabular.
Defaults to \code{"doc_id"}.}

\item{text_col}{Text column name when \code{doc_data} is tabular. Defaults to
\code{"text"}.}
}
\value{
A \link[data.table:data.table]{data.table} with one row per STM topic.
}
\description{
Build a one-row-per-topic interpretation table for an STM fit, combining
NLPstudio's engine-agnostic topic summary with STM-native label columns.
}
\details{
\code{summarize_stm_topics()} keeps \code{\link[=summarize_topics]{summarize_topics()}} as the generic summary
engine and adds collapsed STM-native label columns such as
\code{stm_prob_terms}, \code{stm_frex_terms}, \code{stm_lift_terms}, and
\code{stm_score_terms}. When \code{include_sage = TRUE}, corresponding
\verb{stm_sage_*_terms} columns are added.
}
\examples{
\dontshow{if (requireNamespace("stm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(c(2, 1, 0, 0,  1, 2, 0, 0,  0, 0, 2, 1,
             0, 0, 1, 2,  2, 1, 0, 0,  0, 0, 1, 2),
           nrow = 6, byrow = TRUE),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:6)
colnames(dtm) <- c("growth", "profit", "risk", "loss")
fit <- fit_topic_model(
  dtm,
  engine = "stm",
  model = "stm",
  k = 2,
  control = list(fit = list(seed = 1, max.em.its = 5, verbose = FALSE))
)
summarize_stm_topics(fit, training = dtm, top_n = 3, label_n = 3)
\dontshow{\}) # examplesIf}
}

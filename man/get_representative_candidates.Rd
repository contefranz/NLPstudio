% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{get_representative_candidates}
\alias{get_representative_candidates}
\title{Extract Representative Topic Candidates}
\usage{
get_representative_candidates(
  x,
  data = NULL,
  topics = NULL,
  include_text = FALSE,
  quantile_probs = c(0.25, 0.5, 0.75),
  labels = c("VLOW", "LOW", "HIGH", "VHIGH"),
  doc_id_col = "doc_id",
  text_col = "text"
)
}
\arguments{
\item{x}{A supported topic-model object. This includes \code{nlp_topic_fit},
\code{warp_lda()} output, raw \code{topicmodels} fits, raw \code{seededlda} fits, and
already standardized DTW tables.}

\item{data}{Optional metadata override. When supplied, this is used instead
of metadata embedded in \code{x}.}

\item{topics}{Optional topic filter. May be supplied as numeric indices or
\code{Topic###} identifiers. Filtering occurs after dominant-topic assignment.}

\item{include_text}{Should a text column be attached when text is available?
Defaults to \code{FALSE}.}

\item{quantile_probs}{Numeric vector of cumulative probabilities used to form
candidate bands within each dominant topic. Defaults to quartiles:
\code{c(0.25, 0.50, 0.75)}.}

\item{labels}{Labels used for the candidate bands. Must have length
\code{length(quantile_probs) + 1L}. Defaults to \code{c("VLOW", "LOW", "HIGH", "VHIGH")}.}

\item{doc_id_col}{Document-ID column name when \code{data} is a data.frame or
data.table. Defaults to \code{"doc_id"}.}

\item{text_col}{Text column name when \code{data} is a data.frame or data.table.
Defaults to \code{"text"}.}
}
\value{
A \code{data.table} with one row per document and these core columns:
\itemize{
\item \code{doc_id}
\item \code{topic_max_id}
\item \code{topic_max_value}
\item \code{candidate_band}
\item \code{topic_rank}
}

Metadata columns and optional \code{text} are included when available.
}
\description{
Identify representative document candidates from a topic model by assigning
each document to its dominant topic and banding documents within topic by
their dominant DTW values.
}
\details{
Candidate bands are computed within each dominant topic, not globally across
the corpus. When within-topic quantile cut points collapse because of small
groups or tied values, the function falls back to deterministic rank-based
banding.
}
\examples{
dtm <- methods::as(
  Matrix::Matrix(
    matrix(
      c(1, 0, 1,
        1, 1, 0,
        0, 1, 1,
        1, 1, 1),
      nrow = 4,
      byrow = TRUE
    ),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:4)
colnames(dtm) <- paste0("term", 1:3)

metadata <- data.table::data.table(
  doc_id = rownames(dtm),
  year = 2020:2023,
  text = c("alpha beta", "beta gamma", "gamma delta", "alpha delta")
)

fit <- fit_topic_model(
  dtm,
  engine = "text2vec",
  model = "lda",
  k = 2,
  data = metadata,
  fit_control = list(n_iter = 25, progressbar = FALSE)
)

get_representative_candidates(fit, include_text = TRUE)

}

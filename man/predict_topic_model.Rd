% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topic_model_api.R
\name{predict_topic_model}
\alias{predict_topic_model}
\title{Predict Document Topic Weights for New Data}
\usage{
predict_topic_model(
  x,
  newdata,
  control = list(),
  docvars = FALSE,
  doc_data = NULL,
  include_text = FALSE,
  doc_id_col = "doc_id",
  text_col = "text"
)
}
\arguments{
\item{x}{An object of class \code{nlp_topic_fit}.}

\item{newdata}{New document-feature input. Supported classes are
\link[Matrix:dgCMatrix-class]{dgCMatrix-class}, \link[quanteda:dfm]{dfm}, and
\code{DocumentTermMatrix}.}

\item{control}{A named list of backend-specific prediction arguments.
Defaults to \code{list()}.
\itemize{
\item \code{text2vec}: forwarded to \code{model_object$transform()}
\item \code{topicmodels}: forwarded to \code{topicmodels::posterior()}
\item \code{seededlda}: forwarded to the relevant \verb{textmodel_*()} update call
\item \code{topicmodels.etm}: forwarded to \code{\link[stats:predict]{stats::predict()}} with \code{type = "topics"}
\item \code{stm}: forwarded to \code{\link[stm:fitNewDocuments]{stm::fitNewDocuments()}} for STM fits without
prevalence covariates
}}

\item{docvars}{Should available docvars from \code{newdata} be joined onto the
returned DTW table? Defaults to \code{FALSE}.}

\item{doc_data}{Optional document-data sidecar for metadata or text
enrichment. Accepted inputs are a corpus, data.frame, or data.table keyed
by \code{doc_id}.}

\item{include_text}{Should a \code{text} column be attached when a text-bearing
\code{doc_data} source is available? Defaults to \code{FALSE}.}

\item{doc_id_col}{Document-ID column name when \code{doc_data} is a data.frame or
data.table. Defaults to \code{"doc_id"}.}

\item{text_col}{Text column name when \code{doc_data} is a data.frame or
data.table. Defaults to \code{"text"}.}
}
\value{
A standardized DTW \link[data.table:data.table]{data.table} with:
\itemize{
\item \code{doc_id}
\item topic columns named \code{Topic001}, \code{Topic002}, ...
\item \code{topic_max_id}
\item \code{topic_max_int}
\item \code{topic_max_value}
\item available docvars when \code{docvars = TRUE}
\item optional metadata/text joined from \code{doc_data}
}

Columns are ordered as \code{doc_id}, document metadata, DTW output columns, and
finally \code{text} when text is requested and available.
}
\description{
Predict standardized document-topic weights (DTW) for new documents using a
fitted object returned by \code{\link[=fit_topic_model]{fit_topic_model()}}.
}
\details{
Prediction input is first aligned to the fitted vocabulary stored in \code{x$vocab}.
Terms absent from the fitted vocabulary are dropped with a warning, missing
fitted terms are added as zero columns, columns are reordered to fitted
vocabulary order, and any documents that become empty after alignment are
dropped with a warning.

STM prediction is supported in \code{v0.9.6} only for fits without prevalence
covariates. For STM prevalence-covariate fits, \code{predict_topic_model()} errors
clearly rather than guessing new-document covariate handling.
}
\examples{
\dontshow{if (requireNamespace("text2vec", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dtm <- methods::as(
  Matrix::Matrix(
    matrix(
      c(2, 1, 0, 0,
        1, 1, 1, 0,
        0, 1, 2, 1,
        0, 0, 1, 2),
      nrow = 4,
      byrow = TRUE
    ),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(dtm) <- paste0("doc", 1:4)
colnames(dtm) <- paste0("term", 1:4)

fit <- fit_topic_model(
  dtm,
  engine = "text2vec",
  model = "lda",
  k = 2,
  control = list(fit = list(n_iter = 25, progressbar = FALSE))
)

new_dtm <- methods::as(
  Matrix::Matrix(
    matrix(
      c(1, 0, 0, 1,
        0, 1, 1, 0),
      nrow = 2,
      byrow = TRUE
    ),
    sparse = TRUE
  ),
  "dgCMatrix"
)
rownames(new_dtm) <- c("new1", "new2")
colnames(new_dtm) <- paste0("term", 1:4)

predict_topic_model(fit, new_dtm)
\dontshow{\}) # examplesIf}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k_selection_summary.R
\name{summarize_k_selection}
\alias{summarize_k_selection}
\title{Summarize Topic-Count Selection Results}
\usage{
summarize_k_selection(selection, optop = NULL, include_unsupported = FALSE)
}
\arguments{
\item{selection}{An \code{nlp_k_selection} object returned by
\code{\link[=select_k_topics]{select_k_topics()}}.}

\item{optop}{Optional output from \code{OpTop::optimal_topic()}. The common
\strong{OpTop} result shape with columns \code{topic}, \code{OpTop}, and \code{pval} is
supported, as are cleaned tables with \code{k}, \code{optop}, and p-value aliases
such as \code{p_value} or \code{p.value}.}

\item{include_unsupported}{Logical. Should unsupported aggregate metrics be
retained as \code{NA_real_} columns? Defaults to \code{FALSE}.}
}
\value{
An object of class
\code{c("nlp_k_selection_summary", "data.table", "data.frame")} with one row
per \code{k}. Aggregate metrics are widened into columns. Topic-level rows are
preserved in \code{attr(result, "topic_metrics")} when present. Unsupported
aggregate rows are preserved in \code{attr(result, "unsupported")} when
\code{include_unsupported = TRUE}.
}
\description{
Convert an \code{nlp_k_selection} object into a wide, export-ready table with one
row per candidate topic count.
}
\details{
This helper reports already-computed evidence; it does not fit models, call
\strong{OpTop}, compute a composite score, or choose the best value of \eqn{K}.
Use it after \code{\link[=select_k_topics]{select_k_topics()}} and, optionally, after an external
\code{OpTop::optimal_topic()} call.
}
\examples{
selection <- data.table::data.table(
  k = c(2L, 3L),
  metric = "diversity",
  level = "aggregate",
  topic_id = NA_character_,
  value = c(0.75, 0.82),
  supported = TRUE
)
class(selection) <- c("nlp_k_selection", class(selection))

summarize_k_selection(selection)

}
\seealso{
\code{\link[=select_k_topics]{select_k_topics()}}, \code{\link[=as_optop_input]{as_optop_input()}}
}
